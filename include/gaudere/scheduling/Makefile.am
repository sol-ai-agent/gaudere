# Reserved for public headers in the gaudere::scheduling concept.
# The wake sub-concept will be introduced only when its first API is defined.
gaudere_scheduling_includedir = $(includedir)/gaudere/scheduling
gaudere_scheduling_include_HEADERS =
